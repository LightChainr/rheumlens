# 2026-09-12 两份独立评估：逐条核实与落实

对应文件：`09_交付/独立评估_20260912/重投前独立评估.md`（技术评估）与 `语言精修建议_含英文替换稿.md`（语言精修）。
原则：先修实现、判定规则和数字，再改文字；结果以重跑后的输出为准，不迁就旧文字。

---

## 一、技术评估

| # | 意见 | 核实 | 落实 | 位置 / 证据 |
|---|---|---|---|---|
| 1 | 校准模拟不是折内流程；arm B 两臂 PCA 拟合范围不同；walkthrough 全队列标准化 | **属实** | 新建 `scripts/cohorts/pipeline_core.py`，筛查、增量分析、walkthrough、校准统一调用。两臂为同一函数，只差 `residualise=` 参数。校准在华为云 4 台重跑（每个 γ 300 次）；walkthrough 重跑 | §3.4、§3.5、§9.9、§9.10；Table S1、S2、S10；Figure 6C、8、S1、S2 |
| 1 结果 | — | — | γ=0：分层 19/300=6.3%（95% CI 3.9–9.7，p=0.29），自由 21/300=7.0%（4.4–10.5，p=0.11）。arm B：CMV 形状 ragged 布局损失 0.363（中央 95% 0.272–0.464），包含真实 CMV 的 0.326 | §3.4、§3.5 |
| 2 | γ>0 的拒绝不是 type-I error；自由置换≠无泄漏 | **属实** | 明确两个零假设（自由：全体可交换；分层：层内条件可交换）；γ>0 改称“正确拒绝了假零假设”，并写明“通过检验不能认证疾病生物学”；自由置换不能证明无泄漏 | §2.3、§3.4、§6 表 check 2、§9.6、Figure S1 图注、Cover Letter |
| 3 | 决策树：AUC=1 不是不可估计条件；CMV 不应自动推荐校正；Ren 两个终点 | **属实** | 退化门只保留“无标签混合的采集层”一条；Route A 输出“未检出关联，保留未校正估计”，不推荐校正；Q4 可行性规则固定（某层每类≥5 且总数≥40），Ren 唯一走 Route C；NA 表述为“无信息检验” | §2.4、§6.1、§9.7、Figure 8、Table S10 |
| 4 | 方法表与主筛查代码不一致（填补/编码全队列、内层折数、RF、重复、max_iter） | **属实** | 元数据矩阵改为训练折内填补与编码（未见水平编码为参照水平）；四个 pipeline 分开描述；Table S8 由 `PipelineSpec/ForestSpec` 自动生成（缺项即报错），69 行 | §9.5、Table S8；`tools/build_hyperparameter_table.py` |
| 4 结果 | — | — | 用 `--refresh-design` 只重算元数据列：元数据 AUC 最大变动 0.012；表达列逐位不变；8/9、4/7 不变 | Table 1、Table S4 |
| 5 | CMV 列数≠有效秩；人口学组 BH 后 q 显著 | **属实**（本地复算秩 45/54；按每种子 34 检验 BH，人口学 q=0.020 出现于 2/5 种子） | §4.2 写出秩与 BH 口径；§3.5 写明列数不是有效维数，ragged 布局按真实层大小生成 | §3.5、§4.2 |
| 6 | 数字冲突 9 处 | **全部属实** | 全部按数据刷新：§3.2 扩展模拟数值、Figure 2A/2C 图注、§4.3 最大种子差、§2.4 小队列描述、§5.3 标明 in-sample p、CMV 损失统一、§6 表“自由置换仍有定义” | 各处；`check_manuscript_numbers` 24/24 |
| 7 | 文献 19、30、31、34 书目错误；不能称 new test | **属实** | 19 改为 PMLR 139:8034–8044；30 DOI 改 10.1145/3292500.3330903；31 题名更正；34 补 DOI；42 条全部 Crossref 核对。受限置换归属 Chaibub Neto 等，Cover Letter 不再称“新检验”，新意落在跨疾病实证 | References；§1、§2.3、§7；Cover Letter |
| 8 | 缺逐条回应；Table 1 过密；PDF 字面 `**`；Figure 7A 单图；AI 声明绝对否定；Zenodo 仍为 rc3 | **属实** | 新增 `Response_to_Previous_Review.md`（决定信 + 4 位审稿人逐条）；Table 1 由 15 列减为 12 列；`build_pdf.py` 修正图片插入位置并加 `**` 门禁；正文 “Figure 7A” 改 “Figure 7”；§9.18 列出 Claude Code 与 OpenAI Codex，删除绝对否定句 | 01_UPLOAD；Table 1；PDF |
| 增强 4 | 比较折内 D-only 与 D+X，回答 X 是否超出已记录 D | 采纳 | 新增 `run_incremental.py`，五种子。七个比较 +0.087 至 +0.354；sepsis/COVID −0.001 至 +0.001、influenza 0.000；CMV 联合模型反而比单表达低 0.069–0.136 | **新 §4.5**、Table S5、Table 1 “+ over metadata” 列 |

**未做 / 待作者决定：**
- Zenodo 新版本与 GitHub release：对外发布，需作者确认后执行。§10 仍写现有 Zenodo 记录，发布后应替换为新版本 DOI。
- §9.18 所列 AI 工具与模型版本需作者按实际工作流程核实。
- 评估建议的 “design-shuffle 阴性对照（保留 D–X 结构、仅打乱 D 与标签）” 没有另做；§3.5 以真实 CMV 层大小的 ragged 模拟替代，并在文中写明其局限。
- GSE174188 all-cell 回填 processing metadata 未做（评估列为可选增强）。

## 二、语言精修

| 建议 | 落实 |
|---|---|
| 统一术语（metadata-only / collection-only / demographic-only / sample-quality-only AUC；free / collection-stratified permutation；fixed-hyperparameter pipeline；phenotype label；discrimination/AUC） | 全文执行；“frozen” 仅指 Geneformer；筛查统计量定义处 diagnosis 改 phenotype label，SLE 局部保留 case-control/disease |
| 标题与开篇 | 标题改为 “Recorded metadata predicts the phenotype label in eight of nine public single-cell cohort comparisons”；引言重写并引用 Neto、Spisak、Ferrari 为既有方法 |
| 摘要 / 作者摘要 | 重写（297 / 199 词，符合 PLOS 上限） |
| §2.2 先主统计量、后 V_D | 已调整 |
| 置换保留什么、NA 的具体条件、CMV 不写 clean、未检出≠没有、外部验证不写“两个都错”、不显著只报检验结论 | 已按示范改写（§2.3、§2.4、§4.2、§4.4、§6、§7） |
| 删除修 bug 经历 | §9.8 已删；修正经过只保留在逐条回应文件末节 |
| 章节标题改为陈述发现 | 全部重写 |
| 讨论与结论保持力度 | §7、§8 重写 |
