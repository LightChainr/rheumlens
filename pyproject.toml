[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rheumlens"
version = "2.0.0rc1"
description = "Design validity and external generalisation of donor-level single-cell classifiers"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10,<3.13"
authors = [
  {name = "Hongyu Ying"},
  {name = "Dandan Yun"},
  {name = "Dan Liu"},
]
keywords = [
  "single-cell RNA-seq",
  "foundation model",
  "scGPT",
  "Geneformer",
  "systemic lupus erythematosus",
  "donor-level classification",
  "benchmark",
  "cross-cohort transfer",
  "study design",
  "batch confounding",
  "residualisation",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Bio-Informatics",
  "Topic :: Scientific/Engineering :: Medical Science Apps",
]
dependencies = [
  "numpy>=1.26,<3",
  "pandas>=2.0",
  "scipy>=1.10",
  "scikit-learn>=1.4",
  "PyYAML>=6",
  "joblib>=1.3",
  "tqdm>=4.66",
]

[project.urls]
Homepage = "https://github.com/LightChainr/rheumlens"
Repository = "https://github.com/LightChainr/rheumlens"
Issues = "https://github.com/LightChainr/rheumlens/issues"

[project.optional-dependencies]
io = ["anndata>=0.10", "pyarrow>=14"]
gpu = ["torch>=2.1"]
full = ["anndata>=0.10", "pyarrow>=14", "torch>=2.1", "matplotlib>=3.8", "psutil>=5.9"]
dev = ["pytest>=8", "ruff>=0.6", "mypy>=1.10", "tabulate>=0.9"]

[project.scripts]
rheumlens = "rheumlens.run:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
